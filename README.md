# Ielts-project

Free deployment target: PythonAnywhere.

Main files:
- `flask_app.py` for PythonAnywhere WSGI hosting
- `wsgi.py` as the WSGI entrypoint
- `app.py` for the local standalone server
- `requirements.txt` for dependencies
